# P2 Frozen Final Evaluation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Perform one auditable 640-point frozen-final evaluation of the fixed P2 full-shell method and nine controls.

**Architecture:** Verify the independently audited P2 pilot GO, generate a SHA-bound cutoff-24 final reference cache without reading metrics, then evaluate ten frozen methods over two families and three seeds.  Aggregate deterministic cluster-bootstrap statistics and package a self-contained final evidence archive.

**Tech Stack:** Python 3.12, PyTorch 2.8, CUDA 12.8, RTX 5090 D, pytest, CSV/JSON/tar SHA evidence.

---

- [ ] Add final gate and bootstrap unit tests, including one-shot protections.
- [ ] Add generic final reference-cache command bound to the existing suite SHA.
- [ ] Add exact six-method P5 checkpoint inventory and ten-method evaluator.
- [ ] Enforce 19,200 identities, split/family/seed counts, 2,000 point-cluster
  bootstrap samples, seed statistics, and frozen success criteria.
- [ ] Bind pilot/P5 archives, final suite/cache, source, environment and one-shot
  marker; reopen and audit final evidence before status output.
- [ ] Run full tests and code review, commit and verify clean checkout.
- [ ] Generate final references and execute final exactly once on RTX 5090 D.
- [ ] Return evidence locally; only then generate paper figures and report.
