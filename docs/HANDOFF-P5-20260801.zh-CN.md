# 交接文档 — P5 远端 ROCm 36-run promotion（2026-08-01）

> 给主控大脑。执行机只负责同步、运行与回收证据，不做研究决策。本交接说明
> "交回了什么、当前状态是什么、你接下来应该怎么办"。完整数据见
> [P5-EXECUTION-REPORT.zh-CN.md](P5-EXECUTION-REPORT.zh-CN.md)。

## 交回了什么（一句话）

P5 在远端 AMD MI300X / ROCm 机完整跑完：**36/36 run 完成、0 失败、指标全有限、证据包
SHA-256 核验通过**，最终状态 **`P5_PROMOTION_STOP`**。STOP 是冻结协议给出的**有效负结果**
——候选 ROM 机制没有通过归因与安全门槛，**不是**工程故障，也**不是**可以把 GO 说成 STOP
的含糊结果：`mechanism_go=false`、`gap_scan_non_regression=false`、`promotion_go=false`
都是 JSON 里的明确布尔值。

## 当前权威状态（以此为准）

| 项 | 值 |
|---|---|
| 冻结 commit | `c04c43396a17d0dafe03cb74cf17c96fc5789ef9`（HEAD 一致，工作树干净） |
| 最终状态 | `P5_PROMOTION_STOP` |
| 证据包 | `artifacts/p5-evidence-20260801-092048.tar.gz`，SHA-256 `56891c57…` **OK** |
| 候选机制 | `p5_static_low_rom`（结构低频 Fourier ROM） |
| 测试 | 112/114 通过（2 skip = MPS 专属），见下"本机缺陷与修复" |
| frozen final | **未打开**（STOP 禁止运行 `evaluate_v2_final.py`） |

## 这次 STOP 的科学含义（主控大脑请细读）

1. **候选 ROM 没有证明自己是"物理机制"，只证明了"多花算力/参数有帮助"。** 逐控制对比：
   - 候选近簇误差 0.11018，优于 p5_anchor（0.12323）、p5_wide_anchor（0.11910）等，这是真信号；
   - 但 **p5_long_anchor（训练更久的纯 anchor）0.10616 反而更优** —— 候选对"训练更久"这条
     控制不占优（候选 −3.8%）。这是机制归因失败的**直接原因**：低频 ROM 带来的近簇收益，
     与"把训练时间/算力加倍"不可区分。
   - 候选 gap-scan 误差 0.14920，**劣于**最优非 ROM p5_anchor 的 0.14013（安全门槛
     `gap_scan_non_regression=false`），说明在更广参数扫描上候选还回退了。
2. **结论方向**：不能把"加低频 ROM"包装成论文主创新投稿。当前最强的两个事实是
   (a) 近簇精度最佳其实是 p5_long_anchor 0.10616；(b) ROM 候选在 gap-scan 上回退。
   这两点指向：**下一步要么把"长训练 anchor"当作更强的基准/新候选，要么证明 ROM 在
   某个控制变量下仍有独立于算力的优势**——但这是研究设计，需主控大脑决策，执行机不代劳。

## 主控大脑接下来应该做什么

**应该：**
1. 下载并独立复核证据包：解压 → 对照 `results/p5_promotion/summary.json` 的
   `total_runs=36 / completed_runs=36 / failed_runs=0` → 重算各方法近簇/gap-scan 均值 →
   `sha256sum -c` 确认 integrity。
2. 用 `jq` 复核门控（本机已装 jq 1.6）：
   `jq . results/p5_promotion/diagnostic_gate.json`，重点看 `mechanism_go` 与
   `gap_scan_non_regression`。
3. 决定下一步研究方向：p5_long_anchor 是否上升为新候选？ROM 优势是否需要新的
   控制实验（等算力/等参数下严格对照）才能成立？
4. 更新 `docs/CURRENT-STATUS.zh-CN.md` 到 P5 完成态（当前文档仍是"P5 尚待远端 GPU 执行"）。

**不要做：**
1. **不要运行 `evaluate_v2_final.py`**。P5 未 GO，frozen final 保持关闭；即使未来 GO，
   也先交回研究主机审计，不在执行机自作主张打开。
2. 不要把 `P5_PROMOTION_STOP` 描述为程序错误；也不要把"理论上 ROM 应该更好"写成实测结果。
3. 不要在本机依赖进程退出码判结果（本机 torch 强制 exit 0，见下）。

## 本机缺陷与修复（重跑/审计必读）

1. **CPU LAPACK 缺失**：本镜像 ROCm torch `torch._C.has_lapack=False`，CPU 张量的
   `eigh/eigvalsh/svd/solve/qr/lu_factor` 直接抛 RuntimeError。已用 `tests/conftest.py`
   测试层垫片修复（检测到缺陷且有 GPU 时，CPU linalg 透明搬 GPU 计算后回 CPU）。
   这是**本次新补进仓库的唯一源码改动**，纯测试层，不碰科学逻辑。换带 CPU LAPACK 的
   torch 机器则无需此垫片（垫片在正常环境自动不生效）。
2. **退出码失真**：任何 `import torch` 的进程异常退出都返回 0（torch shutdown 钩子）。
   执行器/审计一律以 `execution-summary.json` 与各 `diagnostic_gate.json` 的 `status`/
   `gate` 字段为准，不信 exit code。
3. **本机缺失工具**：已安装 `jq`（apt 1.6）。
4. **`test_p4.py:330` 有 MPS 专属测试**，在 Linux/ROCm 上 skip（2 个 skip 均为此类，预存在）。

## 证据回传清单（本次已就位）

`artifacts/p5-evidence-20260801-092048.tar.gz`（+`.sha256`）、`p5-executor.log`、
`pytest.log`、`results/p5_execution/execution-summary.json`、
`results/p5_promotion/diagnostic_gate.json`、`results/p5_promotion/summary.json`、
`results/P5_GIT_COMMIT.txt`、`results/p5-pip-freeze.txt`、`results/env-report.txt`。
旧 P4 证据备份在本机 `/mnt/workspace/backup-PINN-PDE-20260801-164457`（未删除）。
